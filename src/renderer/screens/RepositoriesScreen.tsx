import React from 'react'
import { PlaceholderScreen } from './PlaceholderScreen'

export default function RepositoriesScreen(): React.ReactElement {
  return (
    <PlaceholderScreen
      title="Repositories"
      description="Add, remove, and assign repositories to profiles. (Phase 10)"
    />
  )
}
