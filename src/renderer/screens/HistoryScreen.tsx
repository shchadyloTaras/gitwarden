import React from 'react'
import { PlaceholderScreen } from './PlaceholderScreen'

export default function HistoryScreen(): React.ReactElement {
  return (
    <PlaceholderScreen
      title="History"
      description="Browse commit history with paginated load-more. (Phase 16)"
    />
  )
}
