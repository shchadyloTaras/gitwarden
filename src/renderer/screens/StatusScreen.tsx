import React from 'react'
import { PlaceholderScreen } from './PlaceholderScreen'

export default function StatusScreen(): React.ReactElement {
  return (
    <PlaceholderScreen
      title="Status"
      description="Stage and unstage files, view working-tree changes. (Phase 11)"
    />
  )
}
