import React from 'react'
import { PlaceholderScreen } from './PlaceholderScreen'

export default function RemoteScreen(): React.ReactElement {
  return (
    <PlaceholderScreen
      title="Remote"
      description="Fetch, pull, and push with identity safety confirmation. (Phase 14)"
    />
  )
}
